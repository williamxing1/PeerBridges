import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.peerbridges.tutoring',
  appName: 'Peer Bridges',
  webDir: 'public'
};

export default config;
